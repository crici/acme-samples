plugins {
    id 'cpp-library'
}


group = 'com.acme.samples'
version = '0.0.1'

library {
    targetMachines = [
        machines.windows.x86_64,
        machines.linux.x86_64,
        machines.macOS.x86_64
    ]
}
