plugins {
  id 'cpp-application'
}

group = 'com.acme.sample'
version = '0.1.0'


application {
    targetMachines = [
        machines.windows.x86_64,
        machines.linux.x86_64,
        machines.macOS.x86_64
    ]
}
